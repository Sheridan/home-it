---
- hosts: workstations-linux,gate,without-internet
  gather_facts: False
  vars_files:
  - vars/network.yaml

  tasks:

  - name: merge test
    debug:
      var: merged_vars

  - name: ship net test
    debug:
      msg: "{{ query('ship', 'ip', 65536).ipv6.ip }}"

  - name: merge test
    debug:
      var: merged_vars

  # - name: ship net test
  #   debug:
  #     msg: "{{ query('ship', 'delegated_net', item).net }} {{ query('ship', 'delegated_net', item).size }}"
  #   loop: "{{ ['ipv4', 'ipv6'] }}"

  # - name: ship net test
  #   debug:
  #     var: networks.home.subnet
