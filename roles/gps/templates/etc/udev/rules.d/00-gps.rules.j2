# Managed by ansible. Editing is useless
# Sheridan
# Renaming gps interface
# udevadm info -a -n /dev/ttyACM* | grep 'serial'

# u-blox AG, u-blox 7
SUBSYSTEM=="tty", ATTRS{idVendor}=="1546", ATTRS{idProduct}=="01a7", SYMLINK+="gps_u_blox_7"
