#!/usr/bin/python

import subprocess

p = subprocess.Popen('dmesg -wt', stdout=subprocess.PIPE, stderr = None, shell=True)

for line in iter(p.stdout.readline, ''):

    print line

p.stdout.flush()
p.stdout.close()